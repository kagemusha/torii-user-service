import ajaxRequest from '../../../utils/ajax-request';
import { module, test } from 'qunit';

module('Unit | Utility | ajax request');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ajaxRequest();
  assert.ok(result);
});
